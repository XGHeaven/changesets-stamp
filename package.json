{
  "name": "changesets-stamp",
  "version": "0.0.0",
  "description": "Post-Changesets helper that stamps package version placeholders after version bumps.",
  "type": "module",
  "bin": {
    "changesets-stamp": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup src/cli.ts --format esm --dts --clean",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "changesets",
    "version",
    "placeholder",
    "monorepo",
    "release"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@9.9.0",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "fast-glob": "^3.3.3"
  }
}
